const pool = require("../db");

const getBookings = async () => {
    // const result = await pool.query("SELECT * FROM bookings");
    // return result.rows;
};

module.exports = {
    getBookings,
};
